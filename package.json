{
  "dependencies": {
    "axios": "^0.19.0",
    "qs": "^6.9.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/plugin-transform-modules-commonjs": "^7.8.3",
    "@babel/preset-env": "^7.8.4",
    "@babel/preset-typescript": "^7.8.3",
    "@types/node": "^13.7.0",
    "@types/qs": "^6.9.0",
    "babel-jest": "^25.1.0",
    "cross-env": "^7.0.0",
    "eslint": "^6.8.0",
    "jest": "^25.1.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.1"
  },
  "files": [
    "lib",
    "dist",
    "request.d.ts"
  ],
  "scripts": {
    "build": "npm run build:lib && npm run build:dist",
    "build:lib": "npm run clean:lib && npm run build:cjs",
    "build:dist": "npm run clean:dist && npm run build:umd",
    "build:cjs": "cross-env BABEL_ENV=cjs babel src/*.ts --out-dir lib --extensions .ts",
    "build:umd": "cross-env BABEL_ENV=umd babel src/*.ts --out-dir dist --extensions .ts",
    "clean:lib": "rimraf lib",
    "clean:dist": "rimraf dist",
    "format": "prettier --write \"**/*{.js,.ts}\"",
    "lint": "npm run format && eslint --fix --ext \".js,.ts\" .",
    "test": "jest"
  },
  "name": "nuomi-request",
  "description": "axios+jsonp+mock封装的请求库 # 安装 ``` yarn add nuomi-request ```",
  "version": "1.1.0",
  "main": "lib/request.js",
  "typings": "request.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nuomijs/nuomi-request.git"
  },
  "keywords": [
    "nuomi",
    "nuomi-request",
    "request",
    "axios",
    "mock"
  ],
  "jest": {
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  },
  "author": "yinjiazeng",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nuomijs/nuomi-request/issues"
  },
  "homepage": "https://github.com/nuomijs/nuomi-request#readme",
  "directories": {
    "example": "example",
    "lib": "lib"
  }
}
